{
  "name": "sandburgmenu",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "refresh": "python3 scripts/fetch_menus.py --weeks 2 --out sandburg/data",
    "test": "node spotify/test/history.test.js && node spotify/test/unzip.test.js && node spotify/test/apple.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.129.1"
  }
}
